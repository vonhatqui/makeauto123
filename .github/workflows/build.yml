name: Build Vncheat Dylib
on: [push, workflow_dispatch]

jobs:
  build:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Setup Theos & Vendor
        run: |
          export THEOS=$HOME/theos
          git clone --depth 1 https://github.com/theos/theos.git $THEOS
          cd $THEOS
          git submodule update --init --recursive
          ./bin/update-theos
          brew install ldid
          
      - name: Setup SDK (Dùng Git Clone cho chắc)
        run: |
          export THEOS=$HOME/theos
          # Tải bộ SDK trực tiếp từ kho chính chủ, không sợ lỗi file nén
          git clone --depth 1 https://github.com/theos/sdks.git $THEOS/sdks

      - name: Fix Missing dm.pl
        run: |
          curl -LO https://raw.githubusercontent.com/theos/theos/master/bin/dm.pl
          mv dm.pl $HOME/theos/bin/
          chmod +x $HOME/theos/bin/dm.pl

      - name: Compile Vncheat Dylib
        run: |
          export THEOS=$HOME/theos
          cd template
          
          # Tự động tìm SDK iPhone có sẵn trong máy ảo
          # Ưu tiên SDK 16.5 hoặc bất kỳ bản iPhoneOS nào tìm thấy
          SDK_PATH=$(find $THEOS/sdks -name "iPhoneOS*.sdk" -type d | head -n 1)
          echo "Đang build với SDK: $SDK_PATH"
          
          # Lệnh build với cờ chặn lỗi Module cho Xcode 16
          make package FINALPACKAGE=1 SYSROOT=$SDK_PATH \
          ADDITIONAL_CFLAGS="-fobjc-arc -fno-modules" \
          ADDITIONAL_CCFLAGS="-fno-modules"
          
      - name: Upload Vncheat Product
        uses: actions/upload-artifact@v4
        with:
          name: Vncheat-Dylib-Package
          path: template/packages/*.deb





